spring:
  profiles:
  application:
    name: tweet-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 59501

springdoc:
  swagger-ui:
    path: /swagger-ui.html

---

spring:
  profiles: default
  datasource:
    url: jdbc:h2:mem:api
    username: sa